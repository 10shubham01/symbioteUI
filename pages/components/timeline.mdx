import { Steps } from "nextra/components";
import ElementWrapper from "@/components/Theme/ElementWrapper";
import CodeWrapper from "@/components/Theme/CodeWrapper";
import Example from "@/components/timelines/timeline-example.tsx";
import ElementPreviewWrapper from "@/components/Theme/ElementPreviewWrapper";



# Timeline Component

Add sleek, animated transitions to your timeline for a modern, interactive user experience.

## Example

<ElementWrapper
  componentPath="timelines/timeline-example.tsx"
  element={
    <ElementPreviewWrapper>
      <Example />
    </ElementPreviewWrapper>
  }
/>

## Installation

<Steps>
### Install the necessary dependencies:

```bash copy
npm install tailwindcss react-icons framer-motion
```

### Copy and paste the following code into your project.

<CodeWrapper codePath="/timelines/timeline.tsx" />

</Steps>

## Props

|    Prop    |                  Type                   | Description                                                                 |
| :--------: | :-------------------------------------: | :-------------------------------------------------------------------------- |
|   `items`  | `{ heading: string; label: string; description: string; }[]` | Array of timeline items containing `heading`, `label`, and `description` fields. |       |


